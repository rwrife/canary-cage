"""Fingerprints rule pack — heuristic signatures for common AI coding agents.

The default pack lives in :mod:`canary_cage.fingerprints.rules` (JSON).
Operators can drop a ``fingerprints.json`` at the cage root to extend or
override it without touching package code.
"""
